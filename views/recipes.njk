<!DOCTYPE html>
<html lang="pt-br">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/style.css">
        <link rel="shortcut icon" href="https://www.flaticon.com/svg/static/icons/svg/526/526190.svg" type="image/x-icon">
        <title>Foodfy - Receitas</title>
    </head>

    <body>
        <header>
            <nav>
                <a href="/" id="title">
                    Foodfy
                </a>
                <a href="/about" id="about">
                    Sobre
                </a>
                <a href="/recipes" id="recipes">
                    Receitas
                </a>
            </nav>
        </header>

        <section class="recipes">
            {% for item in items %}
                <div class="recipe" id="{{item.id}}">
                    <img src="{{item.image}}" alt="{{item.title}}" class="img-recipe">
                    <div>
                        <h2>
                            {{ item.title }}
                        </h2>
                        <p>
                            {{ item.author }}
                        </p>
                    </div>
                </div>
            {% endfor %}
        </section>

        <footer>
            <p>
                Todos os direitos reservados, receitas Foodfy.
            </p>
        </footer>

        <script src="/main.js"></script>
    </body>

</html>